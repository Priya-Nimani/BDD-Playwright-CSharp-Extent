name: Playwright Reqnroll Tests (ExtentReports Multi-Env)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        env: [staging]

    env:
      TEST_ENV: ${{ matrix.env }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0'

      - name: Restore dependencies
<<<<<<< HEAD
        run: dotnet restore
        
=======
        run: dotnet add package Microsoft.Playwright.NUnit

>>>>>>> 0c50a9a4327cca42a2e57688de10abc249f50256
      - name: Build
        run: dotnet build 

      - name: Install Playwright Browsers
        run: pwsh bin/Debug/net10.0/playwright.ps1 install --with-deps
        shell: pwsh
<<<<<<< HEAD

=======
      
>>>>>>> 0c50a9a4327cca42a2e57688de10abc249f50256
      - name: Run Playwright + Reqnroll Tests
        run: $env:HEADED="0" |
             dotnet test --filter "Category=E2E" 

      - name: Upload Extent Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: extent-report-${{ matrix.env }}
          path: TestReports/ExtentReport_*.html
